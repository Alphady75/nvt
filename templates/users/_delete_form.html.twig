<form method="post" class="d-inline" action="{{ path('users_delete', {'id': user.id}) }}" onsubmit="return confirm('Etes-vous sûre de bien vouloir supprimer ce compte?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-danger"><i class="mdi mdi-account-remove-outline"></i> Supprimer le compte</button>
</form>
