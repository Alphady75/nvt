<form method="post" action="{{ path('pal_europe_delete', {'id': pal_europe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pal_europe.id) }}">
    <button class="btn">Delete</button>
</form>
