{% extends 'base.html.twig' %}

{% block title %}PalEurope{% endblock %}

{% block body %}
    <h1>PalEurope</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pal_europe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pal_europe.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ pal_europe.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pal_europe_index') }}">back to list</a>

    <a href="{{ path('pal_europe_edit', {'id': pal_europe.id}) }}">edit</a>

    {{ include('pal_europe/_delete_form.html.twig') }}
{% endblock %}
